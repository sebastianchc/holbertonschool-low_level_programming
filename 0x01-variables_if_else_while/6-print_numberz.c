#include <stdio.h>
#include <stdlib.h>

/**
 * main - numbers.
 *
 * Return: Always 0.
 */

int main(void)
{
	int num;

	num = 48;
	while (num <= 57)
	{
		putchar(num);
		num++;
	}
	printf("\n");
	return (0);
}
