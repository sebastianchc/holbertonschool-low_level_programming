#include "holberton.h"

/**
 * rev_string - Reverses a string.
 * @s: Variable.
 *
 * Return: Nothing.
 */

void rev_string(char *s)
{
	int x;

	for (x = 0; s[x] != '\0'; x++)
	{
	}
	x = x - 1;

	char y[x];
}
