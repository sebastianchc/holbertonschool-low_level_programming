#include "holberton.h"
#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	int a, z, y, d, x, b, c;

	a = 1052;
	z = 1052;
	for (a = z; a > 0; )
	{
		for (y = 0; d > 10; y * 10)
		{
			for (a = 1052; a != 0; )
			{
				a = a / x;
				b = a % x;
			}
			putchar(b + '0');
			d = z / y;
			c = z % y;
		}
		c = z;
	}
	return (0);
}
